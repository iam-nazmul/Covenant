// Table definitions go here (onchainTable from "ponder"), one file per
// domain area (e.g. policy.ts, position.ts, drawdown.ts, settlement.ts),
// each re-exported below. Empty until contracts/ emits its first event.
export {};
