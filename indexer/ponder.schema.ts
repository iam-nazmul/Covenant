// Ponder auto-loads schema from this file. Table definitions live in
// schema/ per CLAUDE.md's layout ("indexer/schema/ is the training set for
// every future underwriting decision") — re-exported here so Ponder finds
// them at the conventional path.
export * from "./schema";
