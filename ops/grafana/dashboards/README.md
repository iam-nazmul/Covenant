# ops/grafana/dashboards/

Dashboard JSON goes here once there's a keeper, attestation service, or
indexer emitting metrics to watch. Priority candidates when that exists:
cure-window timing (keeper), TCB-stale vs. attestation-failed rate
(attestation), and oracle divergence halts (STACK ADR-5).

Empty — nothing running to dashboard yet.
