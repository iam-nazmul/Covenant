# ops/

Terraform, Grafana dashboards, OpenZeppelin Defender config.

Scaffold only — `terraform/` has no provider or resources yet (infra
target undecided at this pass), `grafana/dashboards/` is empty. Defender
config not started; depends on contracts existing to monitor.
