# proofs/

Halmos symbolic proof specs (STACK ADR-3). Scoped deliberately narrow:
**one invariant, proved** — the exfiltration invariant, not the whole
system. Do not expand scope here without revisiting ADR-3.

Not yet written; depends on `src/vault/` existing. `just prove` will run
these once they exist. Required before anything in `contracts/src/vault/`
merges (CLAUDE.md).
