# models/staging/

One staging model per indexed on-chain event, over `indexer/schema/`
tables in Timescale. New event → indexer handler → staging model ships in
the same PR (CLAUDE.md "Event log").

Empty — no events indexed yet.
